#include <iostream>
#include <cmath>
#include <string>
#include <stack>
#include <vector>
#include "Header_samir+sani-denisov.h"
#include "Header_alina+sani-fedoriko.h"
#include "Count.h"
#include "interface.h"
#define Max_size 1000

using namespace std;

int main()
{

    setlocale(LC_ALL, "RU");

	int type = 0;
	
	//while (1)
	type = interface_cal(); // выдаёт числа и операторы. куда складывать данные??? саня ф?
							// Добавить ещё 20 кнопок, написать красиво их расположение, не забыть сделать норм вывод символов на экран 
							// и прописать ретёрны от кнопок хз куда, наверное в мейн???(Вопрос для Саши Федоряко)


	


	

	/*for (char c : str)
	{
		charStack.push(c);
	}*/

	

	//int type = interface_cal();
	
		

	/*string operation = {};
	float a = 0, b = 0;
	cin >> operation;
	cin >> a >> b;
	Calculate c(operation, a, b); // это вспомогательный калькулятор для обычных чисел. нужен такой же для мнимых (Саня Ф - это ты делаешь с классом итд)
	double s = c.result();		// функции должны к нему обращаться для подсчёта, работает только с рациональными числами.
	cout << s;
	*/
}